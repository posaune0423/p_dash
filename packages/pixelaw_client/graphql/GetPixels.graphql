query GetPixels($first: Int, $where: pixelaw_PixelWhereInput) {
    pixelawPixelModels(first: $first, where: $where) {
        edges {
            node {
                x
                y
                color
                owner
                text
                timestamp
                action
            }
        }
        totalCount
    }
}
